# URL of your backend's Google OAuth redirect endpoint.
# When the user clicks "Continue with Google", the frontend
# will redirect to this URL, which initiates the OAuth flow.
#
# Example for a local Express/Spring/Django backend:
VITE_GOOGLE_OAUTH_URL=http://localhost:8080/api/auth/google
